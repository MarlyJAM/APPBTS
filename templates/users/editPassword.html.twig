{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
        <div class="formulaire  mx_auto  w-75" >
            {{ form_widget(form) }}
        </div>
    {{ form_end(form) }}
{% endblock %}
