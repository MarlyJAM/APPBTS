{% extends 'base.html.twig' %}

{% block title %}nouvelle-reponse{% endblock %}

{% block body %}
<div  class="container-md">
    {{ form_start(form) }}
        <div class="formulaire">
            {{ form_widget(form) }}
            <button class="btn">{{ button_label|default('soumettre') }}</button>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}